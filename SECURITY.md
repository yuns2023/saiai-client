# Security Policy

## 报告安全问题

请使用本仓库的 GitHub Security Advisory 私密报告功能提交漏洞。不要为尚未修复的安全问题创建公开 Issue，也不要在报告中附带真实 API Key、OAuth token、凭据文件、抓包原文或个人信息。

如果需要提供复现材料，请使用最小化的本地 mock 和明确标记的虚构凭据。维护者可能要求通过 Security Advisory 的私密附件或后续私密渠道补充信息。

报告应尽量包含：

- 受影响版本与操作系统；
- 可复现的最小步骤；
- 预期行为、实际行为与安全影响；
- 已做脱敏的日志或测试；
- 你认为可行的缓解方式。

## 支持范围

安全修复面向当前公开 Preview 或最新稳定版本。过期构建、非官方二进制、修改后的 Gateway contract，以及来自第三方分发站的安装包不在支持范围内。

## 项目安全边界

- V2 为独立状态模型，不读取或迁移其他客户端配置。
- Claude 本地代理必须使用安装时生成的 CA 和私钥；不得提交或分发共享私钥。
- API Key 不应出现在 URL、命令行参数、日志、诊断输出、Issue 或截图中。
- Desktop WebView 不直接访问 Gateway；网络验证和凭据存储由 Rust 侧处理。
- Bootstrap 是认证但不产生模型用量的能力发现请求，协议见 [docs/GATEWAY_BOOTSTRAP.md](docs/GATEWAY_BOOTSTRAP.md)。

请勿使用他人的真实账号或凭据进行测试。除非得到凭据所有者对该次操作的明确授权，否则不要发送真实模型请求。

目前没有公开漏洞赏金计划；如未来提供，将以仓库中的正式公告为准。
